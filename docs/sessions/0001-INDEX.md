# Sessions Index

> Append-only. Cada nova sessão adiciona uma linha. Mais recente no topo.

| # | Data | Título | Branch | Commits |
|---|---|---|---|---|
| [0036](0036-android-destravar-e-camera-fullbleed.md) | 2026-07-15/16 | **Android destravado + câmera full-bleed provada no M54 (1º teste em Android físico)**. Roadmap APK preview → teste no Galaxy M54 (Android 16) revelou 5 problemas → 2 fatias. **Fatia 1 (PR #3 MERGEADO):** INTERNET no manifest release (Firebase mudo); SafeArea em 4 telas (edge-to-edge Android 15+ deixava botões intocáveis); câmera morria ao voltar de Config — causa provada no log (`Failed to get Surfaces surfaces=[]`, bind antes da surface) → `bindIfReady`+`pendingConfig` no CameraManager; B2 (token) PULADO (race não se manifestou). **Fatia 2 (PR #4 ABERTO):** câmera full-bleed (decisão de produto, diverge do protótipo — ADR-0027): `_Viewport` Positioned.fill + controles em SafeArea + **fix decisivo 1 linha Kotlin** `PreviewView COMPATIBLE` (SurfaceView furava a tela e ENGOLIA os overlays — era a causa real do "popup cortado"). Golden-com-insets (pumpOnce) inspecionado a cada iteração + **kit adb de inspeção remota** (screencap/wake/stayon — pedido do dono, substitui fotos manuais). **Auditoria 3-lentes pré-fechamento:** 17 achados; corrigidos RecIndicator sob status bar, offsets hardcoded→viewPadding-responsivos (Dynamic Island), Preroll sobre o REC, golden do popup, adendo na spec; 7 gaps de fidelidade pré-existentes = backlog. 331 testes verdes, iOS compila, zero .swift/Pigeon. **NÃO feito:** envio do APK ao cliente (pós-merge #4), Bloco 3 (gravação/foco), validação full-bleed no iPhone. 3 memórias novas. | `feat/camera-native-bridge` → `feat/camera-fullbleed` | `57d2099`..`c72ab68` (PR #3), `ba599ef`..`8e521e5` (PR #4) |
| [0035](0035-bloco-1-firebase-prova-device-crashlytics-dsym.md) | 2026-07-14 | **Bloco 1: prova no DEVICE (iPhone 12) — Crashlytics recebe crash + dSYM subido — GATE §10 FECHADO**. Continuação da 0034 com o iPhone conectado. **Trigger temporário (revertido, NÃO commitado):** botão "CRASH TEST" no splash gated por `--dart-define=RARO_CRASH_TEST=true` → `FirebaseCrashlytics.instance.crash()`; splash parado nessa build (senão some em 1.8s). Pós-prova: `git checkout` do splash → original, analyze limpo + 323 testes verdes + tree limpo. **Build/install (terminal-first §13):** `flutter build ios --profile` assinado (Apple Development, team Y8772MU6JG) → `devicectl install` confirmado "App installed" + container novo ANTES de testar. Dono resolveu 2 bloqueios: device locked + trust do perfil dev. **Prova Crashlytics (gate):** dono tocou o botão → crash → reabriu → painel saiu de "aguardando falha" para **"1 falha não processada"** (doc FlutterFire: crash RECEBIDO, retido aguardando dSYM). **dSYM:** subi os 3 de `Profile-iphoneos/` via `firebase-ios-sdk/Crashlytics/upload-symbols -gsp <plist>` → UUID `A3098D9F...` bateu com o "Ausente obrigatório". **Analytics:** app conectou ao projeto (mesmo `FirebaseAnalytics.instance` do listener). **NÃO feito:** `idevicesyslog` não streou no iOS 26 (tooling) → painel cloud é a prova; upload automático de dSYM em release/CI = pendência menor herdada p/ Bloco 5. **Aprendizado:** verifiquei no pbxproj que o script do flutterfire TEM branch SPM (minha suposição de "aponta pra Pods inexistente" estava ERRADA) — ler a fonte antes de afirmar. **Bloco 1 FECHADO (código+build+device).** docs-only | `feat/camera-native-bridge` | close 0035 |
| [0034](0034-bloco-1-firebase-crashlytics-analytics-codigo-e-build.md) | 2026-07-13 | **Bloco 1: Firebase init + Crashlytics 3 handlers + analytics ligado — CÓDIGO + BUILD provados, device pendente**. Entregável do PLANO-MESTRE Bloco 1 (próximo código, independente da Sensory). **1.1 Firebase:** `flutterfire configure` gerou `firebase_options.dart` (gitignored) — precisou `gem install xcodeproj` na Homebrew Ruby (system Ruby 2.6 não tem → passo iOS morria); `Firebase.initializeApp(options: currentPlatform)` no `main.dart` (era só ensureInitialized → crash em release); plugins gradle DSL moderno `google-services 4.4.4` + `crashlytics 3.0.7` (Context7 stale mostrava 4.3.5/2.7.1 → Google Maven foi o fallback §4). **1.2 Crashlytics:** os 3 handlers (FlutterError + PlatformDispatcher + Isolate), exatamente 3 sem runZonedGuarded especulativo. **1.3 Analytics (era um bug, não "só confirmar"):** `cameraAnalyticsListenerProvider` estava definido mas NUNCA observado (morto); `RaroApp` virou ConsumerWidget + `ref.watch` → agora `camera_started` dispara. **Test fix (systematic-debugging):** wiring quebrou `smoke_test` (`[core/no-app]` sem init); root cause na stack; fix = mock analytics via override (padrão já existente). 323 testes verdes, analyze limpo. **Builds provados:** Android `✓ app-debug.aab` (86.8MB, +SDK Firebase); iOS `✓ Runner.app` (197MB, plist bundlado, firebase-ios-sdk compilou 545s via SPM 16 pins). **Beco de ~15min:** `flutter build ios --simulator` falha (SUPPORTED_PLATFORMS=iphoneos, exit 0 enganoso) — NÃO era Firebase; verificar sempre em `iphoneos --no-codesign` + artefato. **NÃO feito (por design):** prova de crash no DEVICE (gate §10 — decisão do dono "código+build agora, device depois"; runbook em `docs/superpowers/notes/firebase-device-crash-runbook.md`); UI de crash-test permanente (Simplicity); bump 4.4.4→4.5.0 (Surgical). **GATE OK:** os 3 arquivos secret (plist/json/firebase_options) seguem gitignored — nenhum no commit. 2 memórias novas (build iOS iphoneos, flutterfire ruby xcodeproj). 1 commit código `2085811`, 0 `--no-verify` | `feat/camera-native-bridge` | `2085811` |
| [0033](0033-sensory-voicehub-pro-nda-modelo-raro-ptbr-em-validacao.md) | 2026-06-23 | **Sensory reaberto: VoiceHub Pro grátis + NDA assinado + modelo "Raro" pt-BR em build — VALIDAÇÃO ATIVA (zero código)**. Marco comercial/produto, não código. **Frente Sensory (ação do dono, fonte = email Jeff Rogers 2026-06-23, externo ao repo):** a Sensory respondeu o contato e deu **VoiceHub Pro grátis** (expira 2026-09-21); **VoiceHub TEM pt-BR nativo** — o idioma cuja ausência matou o ONNX próprio na 0029. Criado projeto Wake Word `Raro WW PT-BR` (palavra `raro`, Best Quality, output `.snsr`), build **em fila/curso**. Jeff confirmou os 3 pontos que faltavam: licença de **produção non-expiring** (a expiração de ~120d era só do tier de teste; o modelo de teste deeply-embedded expira em 11.43h OU 107 eventos — fato novo do email), **1 preço cobre iOS+Android**, e disposição a modelar preço. **NDA mútuo ASSINADO** (`Sensory Mutual NDA - 6-2026.docx`) — lido cláusula a cláusula: recíproco, 5 anos vigência / 3 anos sigilo, lei CA, **sem** non-use/exclusividade/non-compete/cessão-IP/obrigação-compra; só 2(e) proíbe engenharia reversa (padrão). Resposta ao Jeff redigida (pede modelo flat/per-app vs per-user + ballpark). **Reconciliação anti-drift (workflow 6 agentes, 3 dims+verify; verify pegou 2 erros dos mapeadores + reforçou anti-sobre-correção):** ~12 lugares diziam "Sensory descartado/standby"; escopo do dono = **0033 + alta alavancagem**. Atualizados com proveniência "email Jeff 2026-06-23" e fraseado "validação ativa, NÃO resolvido": `reinject-roadmap.sh` (crítico, bloco Sensory, bash -n+smoke ok), `MEMORY.md` (índice L8/L81), memória `sensory-voicehub-gated` (description + bloco virada + neutralizado "NÃO refazer pesquisa"), `ADR-0023` (bloco 2026-06-23 no topo + status, append). **PENDENTE (2 gates antes de decidir):** (a) PREÇO concreto do Jeff (per-user inviabiliza R$9,90; flat serve) + (b) TESTE do "Raro" no iPhone 12. **NÃO feito (por design):** zero código; ADR NÃO promovido a Accepted; docs de estado vivo de menor alavancagem (02/05/07/ESTADO-WAKEWORD/Blueprint/DoD) deixados p/ a sessão que decidir; append-only respeitado (logs/CHANGELOG/ADR-histórico intactos). **Bloco 1 Firebase segue sendo o próximo CÓDIGO.** 1 commit docs, 0 `--no-verify` | `feat/camera-native-bridge` | reconciliação 0033 |
| [0032](0032-bloco-1-prep-firebase-configs-cli-harness-reconciliacao.md) | 2026-06-23 | **Prep do Bloco 1 (Firebase): conta + configs posicionados + CLI + reconciliação de harness** (NÃO é o Bloco 1 — código não tocado). **Prep (decisão do dono "mover e proteger tudo agora"):** o dono criou projeto Firebase `raro-camera`, registrou apps iOS+Android (`com.rarocamera`), baixou `GoogleService-Info.plist` + `google-services.json` — estavam na **raiz** (risco de commit de chaves reais). Movidos p/ lugares canônicos (`apps/mobile/ios/Runner/` + `apps/mobile/android/app/`), validados, bundle confirmado `com.rarocamera`. **Proteção (`f211d8e`):** `.gitignore` ganhou `**/{GoogleService-Info.plist,google-services.json,firebase_options.dart}`; provado via `git check-ignore` + `git log --all` que **nada vazou** (nem stash/reflog) → sem history-rewrite/rotação. flutterfire CLI 1.4.0 instalado (`~/.pub-cache/bin`, FORA do PATH — `.zshrc` bloqueado pelo classificador, workaround = export na hora). **Auditoria adversarial (workflow 8 agentes, 4 dims + verify):** 7 drifts (0 crítico); verify **refutou 2 fabricações** (auditor inventou bundle ID em AGENTS.md; citou CLAUDE.md:23 velho) e **achou 3 que auditores perderam**. **6 drifts corrigidos:** AGENTS.md:44 `bun --filter` errado→`bun run --filter` (quebrava codegen); PLANO-MESTRE 3 linhas "depende conta Firebase"→pré-reqs prontos (checkboxes 1.1-1.3 mantidos abertos, mecânica gradle DSL corrigida); `block-env.sh` cobre `firebase_options.dart` (provado funcionalmente); `block-infra.md` gitignore marcado feito; `reinject-roadmap.sh` data 06-22→06-23 + bloco "Firebase PRÉ-REQS PRONTOS" (sintaxe+smoke OK). **Chaves são REAIS** (AIzaSy) mas ignoradas+nunca no histórico. **NÃO feito (por design):** código do Bloco 1 (próxima sessão); 2 drifts cosméticos deixados como nota. **Aprendizados:** trabalho fora-de-sessão vira drift invisível; verify adversarial pega fabricação não só erro; SessionStart hook é maior alavancagem. 2 commits, 0 `--no-verify` | `feat/camera-native-bridge` | `f211d8e`, +reconciliação |
| [0031](0031-bloco-0-destravar-android-build-app-id-label-onnx-dormente.md) | 2026-06-22 | **Bloco 0 do PLANO-MESTRE: destravar build Android + App ID + label + ONNX dormente** (1ª sessão de código pós-reconciliação). **0.1 Build Android destravado (`3d24c13`):** `startRecording`/`stopRecording` stub síncrono em `CameraHostApiImpl.kt` lançando `FlutterError(code="sessionFailed")` (fronteira Pigeon → `PlatformException` limpa; cadeia `recording_controller` já trata via `on Object{rethrow}` → falha graciosa). **Drift extra descoberto no 1º build (não no doc):** a regen Pigeon trocou `CameraCapabilities.supportedResolutions/supportedFps` por `supportedFormats: List<FormatCapability>` → `CameraManager.discoverCapabilities` reescrito (matriz 720/1080/4K × 30/60, `requiresPhysicalLens=4K@60` espelhando iOS/ADR-0021). **Gate provado:** `flutter build appbundle` → `app-release.aab` (58.6MB) + 320 testes Dart + 50 contract verdes. **0.2 ONNX órfão MANTIDO dormente** (decisão do dono — negocia Sensory): nada em prod usa os 3 Swift/3 `.onnx` (~2,4MB), mas remover quebraria o `ios_pbxproj_parity_test` e arrisca o build iOS; nota em `Native/Voice/README.md`, histórico em `01a1f67`. **0.3 App ID alinhado ao iOS (`3d24c13`):** `applicationId=com.rarocamera` (dono escolheu igual iOS); `namespace` Kotlin segue `raro_mobile` (ok divergir); descoberto que `AppIdentity.applicationId` no shared **já dizia** `com.rarocamera` — gradle é que violava. **0.4** `android:label`→"Raro Camera". **Extra (`74a6516`) — fechada lacuna de harness levantada pelo dono:** havia `info_plist_parity` (iOS) sem par Android — por isso o drift sobreviveu meses; `android_manifest_parity`→`android_identity_parity` (git mv) cobrindo applicationId (**igualdade exata**, não `contains` — pega regressão com prefixo compartilhado), label==displayName, namespace pode divergir, permissões; **provado red-before-green** (regredir → falha → restaurado). 53 contract + 323 suite verdes. **Aprendizados:** doc do 0.1 era necessário mas não suficiente (gate é o build, não a checklist); recomendação do doc (remover ONNX) teria quebrado contract test → perguntar > seguir cego; `cd` num Bash quebrou restore relativo de red-test (peguei por verificar estado, restaurei via `git checkout`); `test:contract` (gate §10) só roda no pre-push, não no commit. **NÃO feito (por design):** validação Android em device real (stub, sem gravação p/ testar); Bloco 1 Firebase (próximo, depende de conta do cliente). 2 commits, 0 `--no-verify` | `feat/camera-native-bridge` | `3d24c13`, `74a6516` |
| [0030](0030-harness-reconciliation-plano-mestre-entrega.md) | 2026-06-22 | **Reconciliação completa do harness + PLANO-MESTRE de finalização até entrega**. Pedido do dono: alinhar TODA a documentação ao estado real do código para nenhuma sessão futura ler algo falso. **Auditoria de CÓDIGO REAL (3 agentes paralelos):** revelou ~14 drifts + 8 lições não-mapeadas — sprints marcavam RevenueCat/Firebase/i18n como feitos quando são **mock/ausentes** (`subscribe()` só seta bool; `main.dart` sem `initializeApp` → crasharia em release; 0 `.arb`); **Android não compila** (`CameraHostApiImpl.kt` sem `startRecording`/`stopRecording` que o `CameraApi.g.kt` exige). **PLANO-MESTRE criado (`6762312`):** 6 blocos sequenciados (0 destravar/Android · 1 Firebase · 2 RevenueCat · 3 Android paridade · 4 acabamentos · 5 loja); alvo = **iOS+Android paridade total → 2 lojas**. **Reconciliação 29 arquivos (`92fd715`):** notas de estado real em CLAUDE/AGENTS/Blueprint/DoD/05-08/01-04/index; banners nos sprints 1-3; docs perigosos marcados OBSOLETO/SUPERSEDED/REFUTADO (todos reabririam o beco ONNX); hook `reinject-roadmap.sh` 6→11 + bloco ESTADO ATUAL (voz standby, mocks, Android quebrado) injetado em toda sessão; `warn-adr-drift.sh` ganhou paths de voz; CHANGELOG 0.7.0/0.7.1. **Wake-word (decisão do dono):** background ONNX **inviável → STANDBY** (negociando licença Sensory), foreground SFSpeech vigente. **3 lições em memória:** corte-áudio<janela, eval-sintético-não-é-gate, addendum N-modelos-independentes=teto. **Prompt de abertura da próxima sessão criado** (Bloco 0, substitui o de voz obsoleto). **NÃO feito (por design):** zero código de produção tocado; Bloco 0.1 é a próxima sessão. **Instrução verbatim respeitada:** nenhuma pendência de segurança de credenciais documentada. 2 commits, 0 `--no-verify` | `feat/camera-native-bridge` | `6762312`, `92fd715` |
| [0029](0029-s2f-wakeword-onnx-hibrido-voz-real-inviavel-device-revert-sfspeech.md) | 2026-06-21 | **S2.F: treino HÍBRIDO do wake-word "Raro" com voz real do dono → INVIÁVEL no device → revert pro SFSpeech foreground**. Objetivo: fechar o domain gap (modelo 0028 sintético pontuava a voz real ~0.08) injetando a voz real no TREINO. Fallback declarado upfront pelo dono: se falhar, voltar pro SFSpeech "raro gravar"/"raro parar" e finalizar o app. **Processou `Testes.m4a` (126s):** whisper-cpp PT-BR + segmentação por energia → 40 "Raro" + 31 iscas; versionados em `real-audio/` com README. **Validou mecanismo de áudio real na fonte (livekit-wakeword v0.2.1, 2 agentes lendo código):** drop `clip_NNNNNN.wav` 6-dígitos flat ANTES do augment, peso só via duplicação (`trainer.py:291`), 3 armadilhas de naming silenciosas. **Releu Blueprint M03 L461 ANTES de treinar** (alvo = toggle "Raro", sem drift). **Rodada v1 (`f728f14`):** peso 3x + `rounds:2` + n_samples 4000, RunPod A4000 ~$1.15. Gate offline **PIOROU** (Raro 0.079→0.036). **Investigação $0 achou CAUSA-RAIZ: meu `segment.py` cortou 35/40 clips <0.7s** (med 0.56s vs fixtures bons 0.90s) = treino com áudio TRUNCADO + `rounds:2` degradou sintético 81%→64%. Re-cortar 0.9s DOBROU o score offline (provado antes de re-gastar). **Rodada v2 (corrigida):** clips 0.9s + sem rounds + reais peso 10x/n_samples 1500 (~21% pool), RunPod 4000 Ada ~$0.39. Gate offline melhorou (AUC 0.33→0.54, 1º >aleatório; melhor Raro 0.497) **mas 0/41 cruzaram 0.5**. **Gate REAL iPhone 12 (decisão do dono):** build profile assinado + install confirmado (`67D7D0B1`); cadeia OK (`micOk=true`, frames flowing) **mas único pico 0.128 vs limiar 0.35 → ONNX "Raro" NÃO dispara na voz real**. **VEREDITO: 4 modelos (gravar/parar/toggle-sint/toggle-híbrido), nenhum funciona; a palavra "Raro" (2 sílabas, muitas rimas PT-BR) é o limite do pipeline openWakeWord, não a falta de voz real.** **Checkpoint (`01a1f67`)** preservou todo o trabalho ONNX (detector/pipeline/coordinator/modelos/fixtures/docs) antes de reverter. **Revert (`7e9c0c9`):** restaurou voz ao `a43421a` (SFSpeech 0024), removeu 176 linhas de instrumentação de teste, dart analyze limpo, compila (82MB), instalado (`3B931858`); **dono validou no device: log `wake matched: start`+`stop`** — "raro gravar"/"raro parar" voltou a funcionar. **NÃO feito:** background ONNX (inviável, dormente p/ retomar); Android voice parity; finalizar app (próximo foco). **Custo total ~US$1.54 RunPod, 2 pods terminadas (zero cobrança fantasma).** 3 commits, 0 `--no-verify` | `feat/camera-native-bridge` | `f728f14`, `01a1f67`, `7e9c0c9` |
| [0028](0028-s2d-wakeword-raro-toggle-trained-blueprint-drift-fixed.md) | 2026-06-19 → 2026-06-20 | **S2.D: wake-word "Raro" toggle treinado + correção de DRIFT do Blueprint (2 comandos → 1 toggle, ADR-0024)**. Re-treino do wake-word com a correção da 0027 (diversidade 14→80 vozes). **Pesquisa fonte-primária (workflow):** diversidade>volume (grade VoxCPM dá wrap `index%n`; 80 prompts=912 vozes; n_samples acima é repetição → 4000 basta); `--skip-acav` seguro p/ iteração mas FP não-confiável; cortes `cfg_values` 4→2/`timesteps` 3→2/`steps` 40k. **Rodada gravar/parar (skip-acav):** gate iPhone 12 = `raro_gravar` **4/4** (era 0/4! diversidade PROVADA, 0.68-0.84) MAS **cross-fire** (dispara em "parar" g=0.81-0.90); provado que software não salva (argmax 5/8 — detectam "raro qualquer-coisa"). **DESCOBERTA CRÍTICA:** Blueprint M03 (linha 461) + ADR-0009 SEMPRE pediram **toggle "Raro" único**; os ADR-0022/0023 introduziram "dois comandos" contradizendo o Blueprint **sem sinalizar** = drift. Treinei o alvo errado por não reler a fonte. Dono testou Ok Câmera ao vivo: confirma toggle de frase única. **ADR-0024** (toggle único, supersede cláusula 2-comandos). **Rodada "raro" toggle (COM ACAV):** `raro.onnx` recall sint 81.5%, **FPPH 0.0** (ACAV robustece a fronteira p/ background). **ERROS meus registrados:** treinar alvo errado (~US$5, drift não-lido) → memória `feedback_reread_blueprint_before_expensive_train`; **2 rodadas pagas estouraram saldo no SETUP** (ACAV 16GB + pod ociosa ~8h entre setup e treino) → fixes kill-switch `persistent`/START-logo-após-setup/gatilho-onnx-pronto→terminate/pod-sem-ssh-5min=zumbi; facilitei dono colar credenciais no chat (a revogar). **NÃO feito:** refatorar `WakeWordDetector` 2→1 classifier+toggle (próxima sessão, TDD); gravações "Raro" isolado do dono; re-bundle+gate iPhone; wiring background; revogar credenciais. eval sintético NÃO é o gate (já enganou 91%→0%). 1 commit, 0 `--no-verify` | `feat/camera-native-bridge` | `e04eb45` |
| [0027](0027-s2d-wakeword-detector-built-recall-gate-gravar-weak.md) | 2026-06-15 → 2026-06-18 | **S2.D parte 2: `WakeWordDetector.swift` construído (Tasks 0-6, TDD subagent-driven, validado no iPhone 12) + gate de recall offline → `raro_gravar` FRACO**. Pipeline ONNX de 3 estágios completo: `WakeWordPipeline` puro (protocolos testáveis, mel@1280→embedding 76×32 step 8→thresholds 0.34/0.23+debounce+bound 16, 9 testes verdes no device); `OnnxModelSession` (wrapper ORT, 3 assinaturas corrigidas contra API real 1.24.2 — módulo `OnnxRuntimeBindings`, `appendCoreMLExecutionProvider(with:)`, `outputNames:Set`; achou+linkou onnxruntime SPM ausente do Frameworks phase); `WakeWordDetector` fachada (adapters mel x/10+2 + classifier guard `isFinite`/empty + os_log anti-swallow). 4 `.onnx` no pbxproj (fecha débito 0026, 4/4 no bundle device). **Debounce corrigido por cross-check com dump Ok Camera** (`586d4d8`: 25→20 embeddings = 2500ms real, 2 testes de fronteira). **GATE DE RECALL OFFLINE (a peça decisiva, `6c6ac71`):** `WakeWordRecallTests` decodifica WAVs reais (AVAudioFile+Converter, sem tocar mic) → `detector.process()`. 8 fixtures gravadas pelo dono. **Resultado iPhone 12: parar 3/4 ✅ (0.50-0.73), gravar 0/4 ❌ (máx 0.213 vs 0.34 necessário).** Pipeline FUNCIONA; `raro_gravar` é o problema. **Diagnóstico (workflow adversarial + scores device + pesquisa):** CoreML≈CPU (fp16 descartado); mel A/B offline $0 idêntico (mel-context descartado); **causa-raiz via WebSearch+Context7 = falta `custom_negative_phrases`** (configs sem hard negatives; "Raro gravar"/"Raro parar" = caso "prefix bias", solucionável). **Esclarecido: 0.34 (nosso ONNX) ≠ 0.45 (Ok Camera/Sensory, escala diff).** **ERRO GRAVE meu (revertido):** probe de teste DESLIGOU a voz SFSpeech que funcionava + pedi teste antes de confirmar canal de log → dono ficou sem voz; revertido por completo, voz restaurada (container `ACC2B1DB`). Repeti 2 erros já documentados. **NÃO feito:** re-treino do gravar (próximo passo, ~US$3-4 RunPod, hard negatives + lote cheio); bug mel-context (1280→1760, simétrico, baixa prioridade); wiring background (sessão dedicada, NÃO fatia); gate §10 ffprobe. 11 commits, 0 `--no-verify` | `feat/camera-native-bridge` | `5ca45e6`…`6c6ac71` (11 commits) |
| [0026](0026-s2d-wakeword-onnx-feature-extractors-bundled-io-contract.md) | 2026-06-14 | **S2.D parte 2 (preparação): feature-extractors ONNX que faltavam + contrato de I/O validado**. Objetivo era começar `WakeWordDetector.swift`, mas a validação prévia (4 fontes: inspeção do grafo `.onnx` + doc livekit + `utils.py` openWakeWord + notebook interno) revelou que o wake-word é um **pipeline de 3 estágios** (`melspectrogram.onnx` → `embedding_model.onnx` → `<classifier>.onnx`) e que os 2 `.onnx` da 0025 são **só o classifier**. Os 2 feature-extractors genéricos (mel 32 bins / embedding 96-dim) **faltavam** (bug latente: a 0025 baixou só os classifiers do RunPod). **Resolvido:** extraídos do `pip download livekit-wakeword==0.2.1` (MESMA versão do treino, proveniência travada via sha256), cadeia de shapes validada de ponta a ponta (áudio→mel[32]→embed[96]→classifier[16,96]→score), os 4 versionados em `apps/mobile/ios/Runner/Resources/` + contrato de I/O completo em `raro-model-training.md`. Memória nova `raro-pattern-wakeword-onnx-3stage-pipeline-shapes`. **NÃO feito (débito rastreado):** `WakeWordDetector.swift` (adiado p/ S2.D parte 2 real, brainstorm→plano→TDD); **inserção dos 4 `.onnx` no `project.pbxproj`** (sem isso não vão pro bundle / `Bundle.main` retorna nil — adiado p/ tocar pbxproj 1× junto com o detector). **Falhas de processo minhas registradas:** quase pulei o `/session-end` (gate §6 virou pergunta); terceirizei 3 decisões técnicas ao dono que a régua já respondia; débito do pbxproj ficou implícito até o dono auditar. 314 testes verdes, 0 `--no-verify` | `feat/camera-native-bridge` | `e0cd62d`, `f5ca460` |
| [0025](0025-s2d-wakeword-training-runpod-models-92pct.md) | 2026-06-13 | **S2.D parte 1: treino do wake-word "Raro" → 2 modelos ONNX ~92% (RunPod)**. Treinou `raro_gravar.onnx`+`raro_parar.onnx` (`livekit-wakeword` 0.2.1 + VoxCPM PT-BR). **Gate PASSOU** com modelos de PROVA (n_samples=2000): raro_gravar recall **91,4%**/FP-h 0,18, raro_parar **92,2%**/FP-h 0,18 (limiar ótimo). Versionados com README de proveniência em `docs/superpowers/notebooks/modelos-treinados/`. **Kaggle T4 grátis abandonado** (cascata: `command not found`→disco 20GB estoura→`/tmp`→P100 sorteada incompatível sm_60→forçar T4→run 10h+ zumbi RUNNING). **RunPod pago funcionou** (5090 prova/L40S), controle 100% via API GraphQL+SSH. **2 causas-raiz de download achadas:** (a) HF rate-limit anônimo pendura (76h)→`HF_TOKEN`; (b) **backend Xet trava CDN** (CLOSE-WAIT, mesmo com token)→`pip uninstall hf-xet` (1h30 travado→<1s). **Lote cheio (15000) CANCELADO** aos 60% (VoxCPM desacelerou 3-4s/clip, saldo acabaria antes do fim) — dono optou por usar a prova e validar no iPhone primeiro. Pod terminado, custo total ~US$6,40. **Erros registrados (pedido do dono):** estimativas de tempo somadas de memória (dono pegou "~5h" vs 10h30 reais); "RUNNING" lido como vivo sem medir delta (run zumbi); config não-testada sugerida (`wake_phrase`); `set -e`+pip PEP668 mascarou falha; `$SSHOPT` quebrou ssh; gitignore dos .onnx era bug silencioso latente. 2 memórias técnicas novas (`raro-pattern-kaggle-p100-incompatible-force-t4`, `raro-pattern-wakeword-train-runpod-5090-hf-token`) + guia `COMO-TREINAR-runpod-4090.md` com os 3 fixes. **Próximo:** integração iOS `WakeWordDetector.swift` (não existe ainda; onnxruntime já no SPM). 0 `--no-verify` | `feat/camera-native-bridge` | `50eec00`…HEAD |
| [0024](0024-s2c-voice-sfspeech-works-recycle-fix-deadend-refuted.md) | 2026-06-10 → 2026-06-11 | **S2.C voz: SFSpeechRecognizer FUNCIONA — o "beco-sem-saída" da 0023 foi REFUTADO + fix de reciclo + gates anti-repetição**. A 0023 declarou SFSpeech incapaz de wake-word contínuo e decidiu migrar p/ OpenWakeWord (treino+ONNX+CoreML, semanas). Esta sessão **provou que estava errado**: SFSpeech nativo/grátis funciona p/ o escopo aceito (voz com **app aberto na tela**). **Caminho:** começou tentando treinar OpenWakeWord (Colab→Kaggle, `57abed2`/`2685583`/`52fde9b`) → **abandonado** quando o dono questionou a premissa (não há modelo PT-BR público; o grátis já estava no código). Reabilitou SFSpeech (`e33e7aa`) + coexistência voz↔gravação via **2 fontes de áudio** + dono único da AVAudioSession (`c471c40`). **CAUSA-RAIZ REAL (log limpo `pymobiledevice3`):** o "funciona 1×" da 0023 NÃO era limite do endpointer Apple — era reciclar a `recognitionTask` **a cada erro benigno `1110`** (no-speech, emitido por design em silêncio): **521 reciclos vs 1 wake matched** num PID (~6/s), request `nil` na janela morta → "raro parar" descartado; `code=301` era o `cancel()` do próprio reciclo. **Fix `a43421a` (reescrita `VoiceManager.swift` +53/-27, SEM trocar engine):** token de ciclo (mata reciclo órfão) + refresh único agendado + **ring buffer de `CMSampleBuffer` replayado no request novo** (fecha janela morta) + refresh proativo 50s + backoff só em terminais (1101/1107/7/4/203/1700). **Build a6c8bfd falhou ao instalar (exit 1) → dono testou binário VELHO (logs idênticos) — quase lido como "fix não funcionou".** Após install CONFIRMADO via `devicectl`: **PROVA — 8 wake matched (4 start+4 stop), reciclos 521→7, anel destravou.** Dono: "funciona sem parar os 2". **Documentação anti-repetição (pedido do dono):** memória técnica nova `raro-pattern-sfspeech-continuous-no-recycle-per-error`; `raro-pattern-ios-wake-word-no-native-api` CORRIGIDA (diagnóstico beco-sem-saída marcado REFUTADO, histórico preservado); 2 memórias de processo (`feedback_many_native_fixes_means_reread_logs_not_abandon_framework` + `feedback_verify_device_install_before_test`); **2 gates §10** (voz/SFSpeech: verify install + prova de log; transversal: provar incapacidade por log antes de ADR de troca-de-stack); **hook novo** `warn-sfspeech-recycle-per-error.sh` (§8 9→10). **Débitos:** ADR-0023 (OpenWakeWord) tem premissa refutada p/ on-screen — decisão de aposentar é do dono (pendente); own-engine path sem ring buffer (telas sem câmera); notebook Kaggle do treino abandonado commitado (removível); gate §10 ffprobe Frente A (herdado). 7 commits, 0 `--no-verify` | `feat/camera-native-bridge` | `57abed2`…`a43421a` (7 commits) |
| [0023](0023-s2c-preroll-debts-voice-sfspeech-deadend-openwakeword-decision.md) | 2026-06-08 | **S2.C: débitos do pré-roll (código) + voz com SFSpeechRecognizer provou ser BECO-SEM-SAÍDA no device → decisão de migrar p/ OpenWakeWord**. Duas frentes. **Frente A (débitos do pré-roll, código pronto):** A1 combinado salva `raro_<uuid>.mp4` sem infixo `replay_` + teste Dart (`18510ad`); A2 IDR por chunk via AMBAS keyframe keys + `replayFps` real do `config.fps` (`efc4349`+`50b4704`, XCTest PROVADO rodando); A3 retiming da composition ancorado no vídeo + áudio clampado → DTS monotônico (`c04a822`, honesto: passthrough não reescreve DTS → gate §10 decide, fallback re-encode documentado). **Planning hardening:** spec+plan (`2f6bea2`,`e1c5bc3`); **verificação adversarial do plano (workflow 4 agentes) achou 6 blockers corrigidos ANTES de codar** (`148c09a`: enum Pigeon sem sufixo, AVAudioApplication iOS17+ guard, tap idempotente, guarda de mic, Riverpod async-em-build, keepAlive, passthrough-DTS honesto) — pagou-se, impl rodou limpa. **Frente B (voz SFSpeechRecognizer — IMPLEMENTADA mas beco-sem-saída):** ADR-0022 (`43bbb62`)+contrato Pigeon (`d70a327`)+domain (`180652a`)+`RecordingController.toggle` fonte única (`68b2da6`,114 testes)+`VoiceManager.swift` nativo (`2d9929d`,24 XCTests, pegou bug: `VoiceApi.g.swift` fora do target Xcode)+`VoiceController` reativo (`873d2aa`)+indicador (`df699a4`,310 testes)+Volume "em breve" (`55b5c0f`). **CICLO DE DEVICE DEBUG (coração da sessão):** validação no iPhone 12 revelou bugs em cascata que só aparecem no device — crash `installTapOnBus` NSException não-capturável por Swift do/catch → ObjCExceptionCatcher (`d5985fe`); loop `pausado↔escutando` (`019def2`); inflação de backoff (`9169526`); **causa-raiz final (3 análises convergiram + Apple docs + log real via pymobiledevice3):** `requiresOnDeviceRecognition=true` exige stream LIMPO desde o onset → reusar tap entre requests dá `kAFAssistantErrorDomain 1110` em todo request após o 1º (funciona EXATAMENTE 1×); reconstruir engine+tap por ciclo removeu o 1110 mas handoff gravação→voz ainda travava. **INVESTIGAÇÃO DEVICE DO CONCORRENTE iOS (destravou a decisão):** `fmdev.okcamera` via `pymobiledevice3 apps query`+`syslog live` — Info.plist `UIBackgroundModes:['audio']` SEM `NSSpeechRecognitionUsageDescription`; os_log ZERO Speech-framework, usa AudioToolbox/AudioQueue (mic cru)+`Background entitlement:YES`; sessão PlayAndRecord persistente + gravação não toma o mic (`isUsingBuiltInMicForRecording:NO`). **PROVA DE MERCADO: o concorrente NÃO usa SFSpeechRecognizer.** **DECISÃO ADR-0023 (`0deba3c`, adr-guardian GO):** migrar p/ wake-word dedicada on-device **OpenWakeWord/LiveKit-wakeword (Apache 2.0, grátis)** via ONNX+CoreML (Porcupine pago ~US$6K/ano descartado, free tier 3 MAU inútil); background audio + entitlement (modela concorrente, risco App Store aceito pelo dono); supersede só engine do ADR-0022 (contrato Pigeon engine-agnóstico não muda). **Estado seguro (`5c6934c`):** voz SFSpeechRecognizer parqueada (`_voiceEngineAvailable=false`→`VoiceIdle`, indicador auto-escondido, DBG removido). **Gates: analyze 0 · 312 testes verdes · builds ✓.** Voz FUNCIONAL = próxima sessão (OpenWakeWord). **Débito:** gate §10 ffprobe formal da Frente A não rodado isolado (sessão consumida pelo debug da voz). 25 commits, 0 `--no-verify`. **Merge → develop segurado** | `feat/camera-native-bridge` | `2f6bea2`…`5c6934c` (25 commits) |
| [0022](0022-s2b-preroll-embedded-in-rec-feedback-buffer-window-persistence-fix.md) | 2026-06-07 | **S2.B: pré-roll embutido no REC + feedback visível + fix de persistência/sync da janela do buffer**. Liga o 1º gatilho real do replay: apertar REC embute os últimos N s no início do arquivo (copia o concorrente, conserta o feedback). **Ordem não-negociável:** ADR-0003 Addendum 2026-06-07 ANTES do codegen (`8983740`, adr-guardian GO). **Contrato Pigeon** `RecordingOptions.includeReplayPreroll=false` + `CameraFlutterApi.onRecordingStarted` (`4953d5a`); regen 4. **Fix do bug catalogado** `raro-pattern-flutter-async-native-state-needs-notifier`: `RecordingController` deixa de promover `RecordingActive` no await → passa por `RecordingStarting` até `onRecordingStarted` nativo (UI trata starting+active como gravando). **CORREÇÃO da memória de design:** "snapshot no STOP" duplicaria footage (ring enche durante a gravação); websearch (pré-buffer dashcam/HindSight) → pré-roll = N s ANTES do trigger, sequencial. Nativo (`483f38b`): `snapshotChunks()` thread-safe + `pauseAppending`/`resume` + `shouldAppend` puro + `exportCombined` reusando `export()`; `CameraManager` coordena START(snapshot+pausa+onRecordingStarted)/STOP(compõe `[snapshot+G1]`); snapshot vazio/falho/lente trocada = G1 pura. **validator GO** + 3 achados corrigidos (switchLens físico bloqueado no REC; log por chunk; resume no throw). XCTest: `shouldAppend` + `windowChunks` value-copy rodaram. **Feedback** (`67bae21`): `ReplayArmRing` (anel enche em N s, timer client-side honesto) + `PrerollConfirmation` "últimos Ns incluídos". **2 BUGS device-validated pelo usuário** (`6d7bf35`): janela 30s voltava 15s ao sair/voltar + câmera↔Settings dessincronizados — causa: `BufferDuration` DUPLICADO (câmera autoDispose efêmero vs `raro_shared` persistido, enums diferentes). Fix: fonte única no `settingsController` persistido; pill lê/escreve via `toggleBufferDuration`; `setWindow` nos 2 sentidos por listener consolidado; removido enum duplicado (débito 0013); default unificado 30s; 2 testes de regressão cross-tela. Gates: analyze 0 · Dart 290/290 · XCTest verde · `✓ Built 47.1MB`. **Re-validado no iPhone 12 após rebuild: persistência OK + pré-roll OK. Gate §10 ffprobe PASSOU** (4K60 `2160×3840@59.94` + 1080p60, HEVC+AAC, duração prova pré-roll embutido, isReplay:false, frames todos presentes). **Falso alarme registrado:** `tmp/raro_*` é G1 cru intermediário (sem moov standalone), validar o `vault/<id>.mp4`. 3 débitos backlog (áudio-priming ~350ms, DTS emendas, nome `replay_*` cosmético) — não bloqueiam. 6 commits, 0 `--no-verify`. **Merge → develop segurado** | `feat/camera-native-bridge` | `8983740`…`6d7bf35` (6 commits) |
| [0021](0021-s2b-replay-buffer-mechanics-thermal-fix-preroll-research.md) | 2026-06-05 → 2026-06-07 | **S2.B: mecânica do replay buffer (chunked disk-ring) + fix do gate térmico no device + pesquisa do pré-roll-no-REC**. Escopo **fatiado pelo usuário**: mecânica do buffer agora, pré-roll-no-REC (gatilho de produto) na fatia seguinte. **Estratégia validada por websearch** (não memória): chunked disk-ring — deque de chunks `.mp4` ~1s em `temporaryDirectory`, concat no save via `AVMutableComposition`+`AVAssetExportSession` passthrough (ADR-0003 Addendum 2026-06-05, revoga "2 writers rotativos"). **Contrato Pigeon dedicado** `replay_buffer_api.dart` (enable/disable/saveReplay + onReplaySaved/onReplayFailed). **Colisão `PigeonError`** (cada `.g.swift` emite `final class PigeonError` idêntico/internal → 2 no mesmo módulo Swift colidem) → `SwiftOptions(errorClassName:)` **único nos 4 contratos** (ADR-0013 Addendum 2026-06-06 retifica afirmação errada do ADR original; adr-guardian `ADR_AMEND_REQUIRED`). **Nativo:** `ReplayRing` determinístico+XCTest, `ReplayBuffer.swift` writer-por-chunk + save por composition + `finalizationGroup` gating, **fan-out** `RecordingPipeline.swift:205` ANTES do guard (G1 byte-a-byte). **Dart TDD:** state sealed + repo port + `@riverpod` autoDispose Notifier reativo + replay vault sink (`isReplay:true`) + wire `camera_screen` (275/275). Android = stub (Sprint 3). **2 órfãos red-before-green** (vault-race + ref-after-dispose); subagents **pegaram drafts tautológicos** → corrigidos. **BUG TÉRMICO device-validated:** "Replay pausado: aparelho aquecido" a cada cold-start (reabrir/voltar de Settings) com iPhone 12 **frio** mas NÃO pelo pill — causa: gate bloqueava `.serious` igual `.critical` (`.serious` é comum/transitório no A14, agravado pelo build 19min); pill só faz `setWindow` (não recria sessão). Fix: bloquear **só `.critical`** (`c8dadc3`, ADR-0003 Addendum 2026-06-06). **Confirmado no device pelo usuário.** Gate §10: `✓ Built 47.1MB` + install `devicectl` + validação manual. **Dump ADB do concorrente "Ok Câmera"** (Galaxy M54, uiautomator+screencap+`adb input`): modelo "OkReplay" = pré-roll embutido implícito SEM feedback (= a confusão do usuário); background "voz e vídeo"; trial 15d (RARO=30). **Pesquisa+validação do pré-roll-no-REC** (decisão: copiar concorrente, validado antes): GO técnico **com correção** (sonda errou: writer único via insertTimeRange não compila, `append()` só aceita CMSampleBuffer → estratégia = concat no STOP reusando `export()`). 2 memórias novas. 23 commits Conventional, 0 `--no-verify`. **Merge → develop segurado** | `feat/camera-native-bridge` | `cb4f857`…`c8dadc3` (23 commits) |
| [0020](0020-s2b-camera-bugfix-4k60-interruption-thumbnail.md) | 2026-06-04 → 2026-06-05 | **S2.B: correção dos 2 bugs da câmera (4K60 + interrupção) + 1 bug de thumbnail descoberto no device**. Fechou os 2 bugs pré-existentes diagnosticados na 0019, virou 3 (o 3º surgiu na validação device). **adr-guardian ANTES de tocar contrato** (GO sem ADR novo: Bug1 coberto por ADR-0021, Bug2 aditivo; achado: `warn-adr-drift` não cobre `pigeons/`). **Bug1 (4K60, ADR-0021 inteiro):** contrato Pigeon trocou produto cartesiano por `List<FormatCapability{resolution,fps,requiresPhysicalLens}>` combos reais; `discoverCapabilities` lê `device.formats` reais do virtual E da lente física; `selectDevice` escolhe física p/ 4K60; `applyFormat` **falha-alto** (sem fallback silencioso); `setFormat` reconfigura device virtual↔física + restaura zoom virtual; Dart ligou `setFormat` ao `ref.listen` (raiz do bug), HUD dinâmico, 0.5× desabilitado+microcopy em 4K60, Settings filtra catálogo + coage fps=60. **PROVA device ffprobe: 4K60=`3840×2160@60` real**, 4K30/1080p/720p distintos (antes tudo 1080p). **Bug2 (AVError -11847):** flag `isInterrupted` + guards + `CameraErrorCode.sessionInterrupted` novo + removido `onError` espontâneo + `cameraErrorMessage` (TDD) mensagem distinta. **Bug3 (thumbnail some ao reinstalar — descoberto no device):** path absoluto com UUID do container iOS que muda a cada install; fix recomputa `thumbnailPath` por id (igual `filePath`); memória nova. **Auditoria independente:** validator achou item G (setFormat não reaplicava zoom virtual ao voltar de 4K60 — bloqueador, corrigido) + item E (unlock perdido→defer); code-reviewer achou 3 (reconfig em qualquer settings change→gate por format; fps stale→coerção; dead code→removido). **TDD lógica pura** (cameraErrorMessage 4/4, format_catalog 9/9) + **XCTest nativo 10 novos** (capabilities merge/dims + error mapper). Gates: analyze 0 · Dart 255/255 · XCTest exit 0 · iOS Built 46.9MB · **device validado pelo usuário (os 3)**. 2 commits Conventional (camera junta Bug1+Bug2 por contrato entrelaçado; gallery isolado), 0 `--no-verify`. Análise de harness via workflow adversarial (3 lentes→verificação→síntese) | `feat/camera-native-bridge` | `a56a208`, `c393e6c` (2 commits) |
| [0019](0019-s2b-task-b0-recording-pipeline-migration.md) | 2026-06-04 | **S2.B Task B0: migração do pipeline de gravação + 2 bugs diagnosticados + ADR-0021**. Migrou a gravação de `AVCaptureMovieFileOutput` para o pipeline unificado `VideoDataOutput`+`AudioDataOutput`→`AVAssetWriter` (`.mov`→`.mp4`, ADR-0020), pré-requisito do replay. **TDD red-green** na lógica pura (`.mov`→`.mp4`: 240/240) + refactor nativo device-validated (queue dedicada, `startSession(atSourceTime:)`, `finishWriting` async, codec adaptativo). **PROVA no iPhone 12** via dump do vault + ffprobe: 4 `.mp4` HEVC 1080p + áudio AAC reais, thumbnail gera, preview reproduz. **2 bugs PRÉ-EXISTENTES diagnosticados** (systematic-debugging, evidência device — NÃO regressão da B0): Bug1 = `ref.listen(settings)` atualiza `_format` local mas nunca chama `setFormat` (4K sai 1080p; `.mov` antigos também eram 1080); Bug2 = `AVError -11847 OperationInterrupted` ao abrir Settings (sessão interrompida em background) vira "Falha ao gravar". **ERREI e corrigi (honesto):** concluí prematuramente "iPhone 12 não tem 4K60" — pesquisa adversarial (spec Apple 111876) provou que TEM, mas vive na lente física `builtInWideAngleCamera`, não no virtual `builtInDualWideCamera` que o RARO usa pelo zoom contínuo (ADR-0015); as duas não coexistem. **ADR-0021** criado: virtual=default (zoom contínuo=assinatura hands-free), 4K60=opt-in avançado lente física; não-negociável = UI nunca oferece combos impossíveis; forçar via upscale/interpolação descartado (enganoso, App Review 2.3); apps pro (Kino/Filmic/Blackmagic) escolhem física mas público deles ≠ RARO. Escopo fatiado (decisão do usuário): esta sessão = só B0 + decisão; implementação Bug1/Bug2 = próximas sessões (plano `2026-06-04-camera-format-and-interruption-bugs.md`). Aprendizados: `device.formats` de device virtual ≠ capability de hardware; dump do vault+ffprobe > log quando os_log não sai; pesquisa-antes-de-codar evitou fix errado (override codec era boas-práticas, não a causa). Gates: analyze 0 · Dart 240/240 · iOS Built 46.9MB. 3 commits, 0 `--no-verify` (1 barrado por subject-case, corrigido). **Device pendente p/ Bug1/Bug2** | `feat/camera-native-bridge` | `f45cf98`, `2a1c95f`, `539184c` (3 commits) |
| [0018](0018-s2b-preflight-planning-hardening.md) | 2026-06-04 | **Pré-flight S2.B (replay buffer): auditoria de erros + planning hardening** (read-only → docs-only). Antes de codar o replay, auditoria multi-agente (32 agentes, ~2.2M tokens, 4 fases: coleta→pesquisa AVFoundation 2026→verificação adversarial→síntese). Catalogou **60 erros de session logs + 52 de memórias**, **27 guardas vivas**, **10 órfãos** (fix no código, sem trava); **18/22 issues do plano confirmados** adversarialmente (1 refutado). Agente de síntese morreu no limite de sessão → recuperei os 28 outputs estruturados do `journal.jsonl` e sintetizei manual (nada perdido). **DESCOBERTA CENTRAL** (fonte primária, não memória de treino): `AVCaptureMovieFileOutput` + `AVCaptureVideoDataOutput` **NÃO coexistem** numa `AVCaptureSession` (iOS 15+/2026; Apple Dev Forums + prova de existência React Native Vision Camera). Pior: `CameraManager.swift:204` anexa o MovieFileOutput **incondicional** no startSession → adicionar o VideoDataOutput do replay degradaria a gravação G1 já device-validated. `canAddOutput` dá falso positivo. **Pré-condições de docs aplicadas** (escopo aprovado, zero código): **ADR-0020** criado (supersedes 0018 — aposentar MovieFileOutput, pipeline unificado VideoDataOutput+AVAssetWriter p/ gravação E replay, `.mp4` real, ring **encoded** não-raw, queue dedicada, save async); **ADR-0018** → `Superseded by 0020`; **ADR-0003** revisado (impl iOS original marcada inviável + Addendum 2026-06-04 com fragmented MP4 + RAM raw→encoded + gate thermalState); **plano S2.B** Task B reescrita (B0 migrar G1, B2 contrato no `replay_buffer_api.dart` dedicado + saveReplay async, B3 Notifier reativo, B-Gate device) + texto stale corrigido (`bun run --filter`, `recording_controller`, disclaimer drift Task A); **proposta de guardas órfãs** (10, 4 alta sev + gap estrutural XCTests nativos fora do CI). Verificação anti-alucinação: cada claim conferido vs código real (BufferDuration.value, channel replay_buffer, attach:204, vault `.mov`) ✅; diff 100% docs, cross-refs bidirecionais. **VEREDITO: GO-com-condições** — plano não-executável como estava (premissa impossível); agora com a planta correta. 2 rodadas de AskUserQuestion travaram escopo antes de tocar arquivo | `feat/camera-native-bridge` | a criar via `/commit` |
| [0017](0017-thumbnail-gallery-avassetimagegenerator.md) | 2026-06-04 | Thumbnail real na galeria (**capa = 1º frame do vídeo**, ADR-0019). Galeria P07 trocava gradiente HSL por frame real extraído de cada `.mov` do vault. Decisões confirmadas com usuário (Think-Before-Coding): **frame estático** (não vídeo no grid → evita OOM), **remover os 6 mocks** (vault-only), **gerar no save** via **nativo próprio** (sem dep nova — researcher descartou `video_thumbnail` abandonada + `fc_native_video_thumbnail` que exigiria ADR de dep). Gate adr-guardian: tocar contrato Pigeon = ADR → **ADR-0019 commitado ANTES do código**. Pigeon `@async generateThumbnail(videoPath)→jpgPath` + `ThumbnailGenerator.swift` (`AVAssetImageGenerator`, `appliesPreferredTrackTransform`, JPEG q0.8 → `<id>.jpg` ao lado do `.mov`); Android stub no-op (Sprint 3); `thumbnailPath` em metadata/sidecar/`VideoEntity`; `VideoThumbnail` mostra `Image.file` com **fallback gradiente** (Android + vídeos antigos sem `.jpg`); `VaultService.attachThumbnail`+`delete` remove `.jpg`. **2 bugs REAIS de produção pegos por teste de integração de provider** (240 widget tests mockados NÃO pegavam): (1) `ref` após dispose em listener async (fix `ref.mounted` + deps pré-async-gap) (2) race read-during-write no sidecar (fix escrita atômica tmp+rename). **Gates: analyze 0 · Flutter 240/240 · shared 42/42 · XCTest nativo 15/15** (3 do ThumbnailGenerator gerando JPEG de `.mov` real) · iOS `✓ Built 46.9MB`. **Validado no iPhone 12 físico** (gravou vídeo novo → frame real na galeria). 2 commits Conventional, 0 `--no-verify`. 2 memórias novas (ref-dispose + sidecar-atomic) | `feat/camera-native-bridge` | `f2f2712`, `40f4fd3` (2 commits) |
| [0016](0016-sprint2-task-a-recording-vault.md) | 2026-06-03 | Sprint 2 Task A (**S2.A: Recording real + Vault** — 1º entregável do Sprint 2 via subagent-driven-development, 12 tasks TDD, 18 commits). **A câmera virou real:** P05 monta `CameraPreviewWidget` (UiKitView) ao vivo + dirige `CameraController.start/stop` no ciclo de vida; tap REC → `RecordingPipeline.swift` (`AVCaptureMovieFileOutput`, codec HEVC fallback H264 via `setOutputSettings`) grava `.mov` real → `VaultService` (Dart, `Directory` injetável + sidecar JSON) salva em `documents/vault/` → gallery lista via `videoListProvider` (vault-or-mocks) → preview reproduz o arquivo real (`VideoPlayerController.file` fallback `.asset`). **ADR-0018** criado (supersedes ADR-0015 item 7 "sem gravação"; decisão MovieFileOutput vs AssetWriter, `.mov` não `.mp4`, path async via `onRecordingFinished`). Contrato Pigeon `startRecording(RecordingOptions)→sessionId` + `stopRecording()` void + callbacks. `Codec` enum em raro_shared. **Mapper shared→pigeon** (`uhd4k60`→`uhd4k`+`fps60`) — type-mismatch pego pelo implementer (BLOCKED correto). Cada task: implementer + review independente (validator) + perf audit (P05 hot path: `const CameraPreviewWidget` isola PlatformView do timer 1s; REC tap sem I/O de settings, G1-safe). **Gates ready-for-device: analyze 0 · Flutter 218/218 · shared 42/42 · XCTest nativo 12/12 · iOS `✓ Built Runner.app 46.8MB`** (timestamp fresh; recovery SPM Firebase 15-vs-13 via `flutter clean`→`pub:get`). 18 commits Conventional, 0 `--no-verify`. **Device pendente** (G1 <300ms + reprodução real = parte do usuário). Out-of-scope S2.B+: replay/wake/volume/revenuecat/share/analytics-recording. Merge → develop segurado) | `feat/camera-native-bridge` | `0eadcb9`…`f4e5a53` (18 commits) |
| [0015](0015-sprint1-task-h-closure.md) | 2026-06-03 | Sprint 1 Task H (**closure**: smoke test fim-a-fim das **12 telas validadas no iPhone 12 físico** — build profile assinado via terminal (2 git overrides SPM) + `devicectl` install/launch; usuário confiou no perfil de dev (free Apple ID). Fluxo completo sem crash, sem regressão câmera nativa: splash→onboarding 1/2→permissions→câmera (preview preto = MOCK esperado, `UiKitView` só Sprint 2)→REC/timer→settings→gallery→**preview vídeo tocando + scrubber rainbow + seek**→popup M01→paywall 2 planos→checkout Apple Pay→confirma→câmera→trial "30 dias". **1 fix device** (vão vertical Resolução em Settings: `GridView.count childAspectRatio 3.4` forçava células ~48px vs chip ~33px → 2 `Row`s com `Expanded`; design-fidelity 6/6 PASS, 19/19 GREEN). **1 fix infra** (`Package.resolved` divergente `.xcodeproj` firebase 12.13 stale vs `.xcworkspace` 12.14 → `Couldn't check out revision`; removido stale, xcodebuild regenerou 12.14). Suíte 200/200. **Merge Task C SEGURADO por decisão do usuário** (gates G1/G7 perf + Android M54 + goldens → Sprint 2/3). Blueprint §11 Sprint 1 ✅. Aprendizado: build iOS background dá exit 0 enganoso com `.app` velho) | `feat/camera-native-bridge` | `c2fba15`, `0bb6de9` (+ docs closure) |
| [0014](0014-sprint1-task-g-preview-paywall-checkout.md) | 2026-06-02 | Sprint 1 Task G (walking skeleton: P08 Preview + M01 Subscription popup + P09 Paywall + P10 Checkout + trial countdown — 4 telas/overlay via TDD usando enums do contrato `AppScreen`/`AppModal`. **Pré-requisito:** ADR-0017 + Blueprint §2.7.1 + `video_player ^2.11.1` (validado pub.dev+Context7); clipe `.mp4` mock gerado via ffmpeg; provider `autoDispose`+`ref.onDispose`. Preview com scrubber rainbow custom + `PreviewMetadata` (H.265/size determinístico). Contrato `raro_shared`: `PlanPricing` (9.90/89.90/7.49) + StorageKeys subscription/trial. M01 popup auto 450ms se `!subscribed`; `SubscriptionController` keepAlive via port `SubscriptionStore`; copy **30 dias** (invariante ADR-0010, protótipo diz 15). Paywall 2 cards + features nos cards + rodapé legal + watermark; Checkout tiles Apple/Google Play (ícone multicolor custom) + CTA disabled→confirma→`subscribe(now)`→câmera. Trial countdown em Settings. Router stubs→reais + `/checkout` + `_ScreenStub` removido. **design-fidelity 2 rodadas → 10/10 fixes** (2ª pegou 2 erros meus: scrubber branco→rainbow, radius 16→22 fiel ao HTML). Suíte 155→200 GREEN + golden + shared 39/39. **Device pendente p/ Task H**) | `feat/camera-native-bridge` | `5f5c5bb`…`aca3aa9` (6 commits) |
| [0013](0013-sprint1-task-f-settings-gallery.md) | 2026-06-02 | Sprint 1 Task F (walking skeleton: P06 Settings + P07 Gallery — 2 telas Flutter via TDD, substituindo os stubs da Task E; Settings com `RecordingSettings` freezed persistido via `SharedPreferencesAsync` (API moderna 2026, validada Context7) através de port `SettingsStore` mockável; estabilização = status fixo conforme protótipo; idioma só persiste preferência (i18n Sprint 3); onboarding **modernizado** pro mesmo padrão async via port (3 testes regressão GREEN); Gallery com 6 `VideoEntity` mock em grid 3-col, thumbnails por gradiente HSL (sem PNG), filtros client-side Todos/Hoje/Esta semana/Raro Replay com lógica pura testável; router stubs → telas reais + stubs paywall/preview pra Task G; **contract test pegou hex fora de core/theme** → movido pra `RaroAccents`; suíte 122→155 GREEN; design-fidelity 13/13 PASS; device pendente p/ Task H) | `feat/camera-native-bridge` | `46d9635`…`423ad35` (3 commits) |
| [0012](0012-sprint1-task-e-permissions-camera-shell.md) | 2026-06-02 | Sprint 1 Task E (walking skeleton: P04 Permissions + P05 Camera UI shell — 2 telas Flutter via TDD com Riverpod 3; P04 via `PermissionGateway` port mockável + `permission_handler`; P05 com HUD res/fps/lens, REC button mock + timer fake, buffer pill, lens switcher (preview MOCK — UiKitView nativo só Sprint 2, REC inline sem Lock mode); best-practices validadas via Context7+WebSearch; design-fidelity → 2 fixes (hud lens ascii x, buffer pill active) + **validado no iPhone 12** → 3 fixes device (rec glow sutil, grad-line topo, ícone câmera); suíte 122/122 GREEN; 3 memórias novas (late-final AnimationController dispose crash + pumpAndSettle timeout em repeat infinito + BoxShadow opacity glow bruto)) | `feat/camera-native-bridge` | `2c75f4a`…`af74c01` (6 commits) |
| [0011](0011-sprint1-task-d-splash-onboarding.md) | 2026-06-02 | Sprint 1 Task D (walking skeleton: P01 splash + P02/P03 onboarding + go_router — 3 telas Flutter navegáveis via TDD com Riverpod 3 providers keepAlive + go_router; fontes Space Grotesk/Inter/JetBrains Mono bundladas (VF) + logo + tokens de gradiente centralizados; design-fidelity PASS; **validado no iPhone 12** (build profile assinado): 2 fixes pegos em device — buffer fill layers P03 + glow drop-shadow vs BoxShadow no splash; 1 memória nova) | `feat/camera-native-bridge` | `bc5e6ed`…`22e0462` (7 commits) |
| [0010](0010-sprint1-task-c-camera-focus-ring.md) | 2026-06-01 | Sprint 1 Task C (camera G4: focus ring validado no iPhone 12 — 3 bugs empilhados corrigidos via systematic-debugging + TDD + workflows 3-ângulos: opacity model=0 invisível, tap caindo no vão → re-arquitetado pro nativo (UITapGestureRecognizer), ring vindo do canto (anchor/bounds); + desbloqueio build iOS sandbox (bun filter, SPM 2-gate git override, profile vs debug); 5 memórias + hook novo + CLAUDE.md §13/§8. **Merge SEGURADO** — gates G1/G7 perf + Android M54 + goldens → Sprint 2/3) | `feat/camera-native-bridge` | `8d4a5d9`, `b8b9a01`, `42bb901` (3 commits) |
| [0009](0009-sprint1-task-b-workflow-refactor.md) | 2026-05-29 | Sprint 1 Task B (workflow refactor: CLAUDE.md §6 → "1 sessão = 1 entregável"; §11 cortada 29→11 por critério, 18 viram memória/hook + 1 memória nova Pigeon; Blueprint §11 roadmap 3-Sprint com checkboxes reais; session 0007 retroativo criado) | `feat/camera-native-bridge` | `8292ded`, `8442dea` (2 commits) |
| [0008](0008-sprint1-task-a-cleanup.md) | 2026-05-29 | Sprint 1 Kickoff + Task A (audit FASE 1–4 de 26 deps/APIs via Context7+WebSearch + 6 telas de gate; MD audit fixes; cleanup: 6 memórias renomeadas consertando índice, multicam mantido p/ não quebrar refs em docs, §8 → 9 hooks reais; 0007 reservado p/ Sprint-0 reset retroativo na Task B) | `feat/camera-native-bridge` | `7608c59`, `4f9085e` (2 commits) |
| [0007](0007-sprint0-reset-roadmap-workflow.md) | 2026-05-29 | sprint 0: master plan v2 + 3 sprint MDs (reset estratégico — Fases v1 → 3-Sprint; cleanup com critério; Riverpod providers vs MockData; free Apple ID até Sprint 3) | `feat/camera-native-bridge` | `06ec0fb`…`1160c9f` (4 commits) |
| [0006](0006-camera-task-19-focus-perf.md) | 2026-05-28 → 2026-05-29 | camera task 19 focus + perf (G4 focus ring nativo via CALayer + 5 root causes tap-to-focus delay no iPhone 12 colapsados: smoothAutoFocus, debounce 100→16ms, EagerGestureRecognizer, KVO permanente, setNeedsDisplay; terminal-first iOS workflow §13; 3 workflows audit ~60 agents 2.6M tokens; ADR-0016 harness E2E híbrido Proposed; decisão $99 Apple Dev em 30d) | `feat/camera-native-bridge` (extensão Task 19, mesma branch) | `cba16ce` … `3b79021` (6 commits) |
| [0005](0005-camera-device-validation.md) | 2026-05-27 → 2026-05-28 | camera device validation (Task 19 — iPhone 12 físico: 11 bugs corrigidos, 4 memórias novas, ADR-0015 addendum A-H, bootstrap permission script, G1-G6 ✅ + G8/G9 debug-only) | `feat/camera-native-bridge` (extensão Task 19, mesma branch) | `17d6d45` … `3e14d8f` (24 commits) |
| [0004](0004-camera-native-bridge.md) | 2026-05-26 / 2026-05-28 | camera-native-bridge (P05 fundação + device validation Task 19: preview + lens 0.5×/1× sem blackout + focus + format + permission flow + observers bg/fg, ADR-0015 com addendum 2026-05-28 seções A-H) | `feat/camera-native-bridge` (validação iPhone 12: G1-G6 ✅, G7/G10 ⏳ Instruments, G4 nativo CALayer, G8/G9 lifecycle real → TestFlight Apple Dev Program) | `6104289` … `62a1c2b` (48 commits) |
| [0003](0003-flutter-3.44-spm-migration.md) | 2026-05-26 | flutter-3.44-spm-migration (upgrade SDK + SPM + iOS 15, ADR-0014) | `feat/flutter-3.44-spm-migration` → merged em `develop` (`d91ccaa`) | `b357d4b` … `d91ccaa` (17 commits) |
| [0002](0002-api-contract-shared.md) | 2026-05-26 | api-contract-shared (rm-2 spec bloqueante, 12 famílias) | `feat/api-contract-shared` | `2783166` … `f30bb1b` (16 commits) |
| [0001](0001-bootstrap.md) | 2026-05-25 | Bootstrap do projeto (Fases 1–5 + 3 sprints de fixes) | `develop` | `c40e55d` … `1c43be0` (33 commits) |

## Próxima sessão (BLOCO 0 do PLANO-MESTRE — destravar e estabilizar)

**ATUALIZAÇÃO 0030:** o harness foi totalmente reconciliado com o estado real. Roadmap vigente = [PLANO-MESTRE-finalizacao-entrega-cliente.md](../superpowers/plans/PLANO-MESTRE-finalizacao-entrega-cliente.md) (6 blocos, alvo iOS+Android paridade total → 2 lojas). **Prompt de abertura pronto:** [NEXT-SESSION-PROMPT-bloco-0-destravar.md](NEXT-SESSION-PROMPT-bloco-0-destravar.md) (usar este; o de voz/openwakeword está OBSOLETO). **Entregável da próxima sessão = Bloco 0:** (0.1) destravar build Android — implementar `startRecording`/`stopRecording` no `CameraHostApiImpl.kt`, critério `flutter build appbundle` passa; (0.2) decidir wake-word ONNX órfão (remover do bundle, preservado em `01a1f67`); (0.3) **[GATE — perguntar ao dono]** App ID Android divergente `com.rarocamera.raro_mobile` vs `com.rarocamera` (imutável pós-publicação); (0.4) `android:label` → "Raro Camera".

**Estado autoritativo do wake-word:** foreground SFSpeech ("raro gravar"/"raro parar") FUNCIONA no iPhone 12; background ONNX **inviável → STANDBY** (dono negociando licença Sensory) — NÃO reabrir treino ONNX/OpenWakeWord sem ADR. Memórias-chave: `feedback_synthetic_eval_is_not_the_gate_device_is`, `feedback_verify_device_install_before_test`, `feedback_ios_workflow_terminal_first_no_xcode_build`, `raro-pattern-bun-filter-arg-order`.

---

### Histórico (pré-0024 — pré-roll FECHADO e device-validated)

**Pré-roll embutido no REC FECHADO E PROVADO** (sessão 0022, com Gate §10 formal): apertar REC embute os últimos 15/30s num `.mp4` único, feedback visível (anel enchendo + "últimos Ns incluídos"), estado por callback nativo `onRecordingStarted`, janela do buffer com fonte única persistida câmera↔Settings. **Re-validado no iPhone 12 após rebuild:** persistência da janela OK + pré-roll OK. **Gate §10 ffprobe PASSOU** (2 amostras: 4K60 `2160×3840@59.94` + 1080p60, HEVC+AAC, duração prova pré-roll embutido, isReplay:false, todos os frames presentes — `nb_read_frames` bate com 60fps, decode exit 0). Detalhes na "Atualização 2026-06-07" do log 0022. **Falso alarme registrado:** o `tmp/raro_*` é o G1 cru intermediário (sem moov standalone, descartável); o artefato a validar é o `vault/<id>.mp4`.

**Débitos conhecidos do combinado (backlog, decisão do usuário — não bloqueiam):**
1. **Áudio-priming ~350ms** no início do pré-roll (1º chunk sem áudio; sem drift progressivo depois — vídeo+áudio terminam alinhados). Mitigação: `kCMSampleBufferAttachmentKey_TrimDurationAtStart` no 1º chunk.
2. **DTS não-monotônico nas emendas** dos chunks de 1s (warning cosmético de mux; decode/playback OK). Mitigação: IDR por chunk (`AVVideoMaxKeyFrameIntervalKey`) + retiming na composition.
3. **Cosmético:** combinado salvo no vault como `replay_<UUID>.mp4` em vez de `raro_*` (artefato do nome em `ReplayBuffer.export():330` + strip de `raro_` em `_idFromPath`). 1 linha, sem efeito funcional.

**Próxima fatia de produto = voz** ("Raro" aciona o MESMO save, camada POR CIMA do botão — não no lugar; ler `raro-competitor-okcamera-replay-model` estratégia em camadas + `raro-pattern-ios-wake-word-no-native-api`: SFSpeechRecognizer tem limites de hora/sessão, workaround = transcript matching + restart loop). **Backlog Sprint 2:** volume button, RevenueCat sandbox, share, analytics + os 3 débitos acima. **Pendências de processo:** XCTests no CI + device gate no `/verify-slice`. Android nativo do replay = Sprint 3.

> **⚠️ Build iOS no sandbox (revisado na 0015):** além dos 2 git overrides do SPM (CLAUDE.md §13), atenção a `Package.resolved` divergente entre `.xcodeproj` e `.xcworkspace` — se o build falhar com `Couldn't check out revision`, comparar a revision firebase dos DOIS arquivos; o do `.xcodeproj` pode estar stale. Fix: remover o stale, deixar xcodebuild regenerar. Build iOS em background dá exit 0 enganoso com `.app` velho — sempre conferir `✓ Built ... .app` no log + timestamp do `.app`.

> **Task H fechada na sessão 0015:** smoke test fim-a-fim das 12 telas no iPhone 12 ✅ sem crash/regressão. 1 fix device (vão Resolução em Settings, `c2fba15`) + 1 fix infra (`Package.resolved` firebase 12.13→12.14, `0bb6de9`). Preview preto na câmera = MOCK esperado (Sprint 2 liga o `UiKitView`). **Merge `feat/camera-native-bridge` → `develop` SEGURADO por decisão do usuário** — gates G1/G7 perf + Android M54 + goldens abertos; reavaliar quando fecharem (Sprint 2/3). Suíte 200/200 GREEN.

> **Task G fechada na sessão 0014:** P08 Preview + M01 popup + P09 Paywall + P10 Checkout + trial countdown via TDD (45 testes líquidos, suíte 155→200). ADR-0017 `video_player ^2.11.1` + clipe mock; provider autoDispose. Contrato `PlanPricing` + StorageKeys subscription/trial. Copy **30 dias** (invariante, não os 15 do protótipo). design-fidelity 2 rodadas → 10/10 fixes (2ª pegou scrubber rainbow + radius 22/20.5 fiel ao HTML). `_ScreenStub` removido do router. Blueprint §11 P06/P09/P10/P11/trial ✅. **Device pendente** (é a Task H). Débito Sprint 2: RevenueCat real (swap do port `SubscriptionStore`).

> **Task F fechada na sessão 0013:** P06 Settings + P07 Gallery via TDD (33 testes líquidos, suíte 155/155). Persistência **modernizada** pra `SharedPreferencesAsync` via port `SettingsStore`/`OnboardingStore` (swap-able Sprint 2). Estabilização = status fixo; idioma só persiste preferência. Gallery com gradiente HSL (sem PNG) + filtros client-side. design-fidelity 13/13 PASS. Blueprint §11 P07/P08 ✅. **Device pendente** (validação física do fluxo completo é a Task H). Débito Sprint 2: `BufferDuration` duplicado (`raro_shared` vs. `camera_shell_state.dart`).

> **Task E fechada na sessão 0012:** P04 Permissions + P05 Camera shell via TDD (29 testes novos, suíte 122/122). P04 usa `PermissionGateway` port (mockável). P05 com preview **MOCK** (UiKitView nativo só Sprint 2), REC inline (sem Lock mode P05a), lens switch = estado local. design-fidelity → 2 fixes + **validado no iPhone 12** → 3 fixes device (rec glow, grad-line topo, ícone câmera) aprovados pelo usuário. Blueprint §11 P04/P05 ✅. 3 memórias novas (AnimationController dispose / pumpAndSettle / BoxShadow opacity glow).

> **Task C (camera G4) parcialmente fechada na sessão 0010:** focus ring iOS validado em device. **Merge segurado** — gates G1/G7 (perf) + Android M54 + goldens deferidos pra Sprint 2/3 (ver spec `2026-05-28-camera-task-19-closure-design.md` §progresso). Branch `feat/camera-native-bridge` NÃO mergeada em `develop`. Backlog Sprint 3: tap-to-focus nativo Android (CameraX/Kotlin).

> **Nota (Sprint 0 reset):** as antigas "Opções A/C/D" (harness E2E híbrido, replay buffer, Patrol) viraram backlog de Sprint 2/3 no roadmap (Blueprint §11). Sprint 1 foca cleanup + walking skeleton iOS. ADR-0016 e a spec do harness E2E continuam válidos como referência para Sprint 2.

## Como retomar (prompt sugerido)

Cole no início da próxima sessão (S2.B — voz, pré-roll já fechado):

> Sprint 2 Sessão S2.B continuação — fatia VOZ. CONTEXTO: o pré-roll embutido no REC fechou e foi PROVADO na sessão 0022 (Gate §10 ffprobe passou no iPhone 12: 4K60 `2160×3840@59.94` + 1080p60, HEVC+AAC, duração prova pré-roll embutido, todos os frames; persistência da janela re-validada no device). Tudo commitado em `feat/camera-native-bridge` (`8983740`…`6d7bf35` + close). **Próxima fatia = VOZ:** "Raro" aciona o MESMO save do pré-roll (camada mágica POR CIMA do botão REC, não no lugar — o botão continua sendo o gatilho base; a voz é um 2º gatilho pro mesmo fluxo). **Ler PRIMEIRO:** memória `raro-competitor-okcamera-replay-model` (estratégia em camadas: botão→voz→volume), `raro-pattern-ios-wake-word-no-native-api` (SFSpeechRecognizer: ~1000 req/h, 1 min/sessão, SEM API de wake word → workaround transcript matching + restart loop), ADR-0009 (wake word = "Raro"), contrato Pigeon `voice_api.dart` (já existe stub). Wake word é `"Raro"` (NUNCA "OkCamera"). **Débitos do combinado em backlog** (não bloqueiam, não atacar salvo se pedido): áudio-priming ~350ms, DTS nas emendas, nome `replay_*` cosmético. Setup: `/prime` → `git log`/`status`. Lembrar: merge → develop segurado; XCTest nativo NÃO roda em CI (device obrigatório); build iOS terminal-first + 2 git overrides SPM; `flutter build ios` cru reintroduz Firebase 15-vs-13 → recovery `flutter clean`→`pub:get`(fix-spm)→build; exit-0 de build background é enganoso, conferir `✓ Built`+timestamp; build profile (não debug) p/ device; `bun run --filter @raro/mobile <script>` (filter DEPOIS de run); `raro_shared.BufferDuration` usa `.value`; **no gate §10, validar o `vault/<id>.mp4`, NUNCA o `tmp/raro_*` (G1 cru intermediário sem moov standalone).**

## Recovery rápido (se algo quebrar ao retomar)

| Sintoma | Comando |
|---|---|
| Build iOS quebra com "Firebase iOS 15 vs 13" | `bun --filter @raro/mobile run bootstrap:ios` |
| Câmera para de aparecer em Ajustes iPhone | `bun --filter @raro/mobile run bootstrap:ios` + Xcode Clean Build Folder + reinstall |
| Build "BUILD SUCCEEDED" mas app não roda | `xclogparser parse --file <log>.xcactivitylog --reporter flatJson` (ver memória `raro-pattern-xcode-preaction-modifies-workspace`) |
| Xcode "Failed to launch — code signature" em release | Edit Scheme → Run → Build Configuration = **Debug** (free tier não suporta release no device, ver memória `raro-pattern-flutter-debug-vs-release-on-device`) |
| Tela "iOS 14+ debug mode" ao reabrir app no iPhone | **Não é bug**; é restrição arquitetural Apple+Flutter. Use Control Center / multitasking parcial. |
