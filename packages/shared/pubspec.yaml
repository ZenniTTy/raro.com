name: raro_shared
description: "Shared constants, enums and event names for the Raro Camera app (Dart-only, no Flutter dependency)."
publish_to: "none"
version: 0.2.0

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  meta: ^1.16.0

dev_dependencies:
  test: ^1.30.0
  lints: ^6.0.0
